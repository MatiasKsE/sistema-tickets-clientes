# MongoDB Configuration
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/sistema_tickets_clientes?retryWrites=true&w=majority
DB_NAME=sistema_tickets_clientes

# Server Configuration
PORT=8002
NODE_ENV=production

# JWT Secret
JWT_SECRET=tu_jwt_secret_muy_seguro_aqui

# Data Directories (for file uploads and tickets)
DATA_DIR=./database

